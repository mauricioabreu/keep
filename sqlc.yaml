version: "2"
overrides:
  - go_type: "github.com/google/uuid"
    db_type: "uuid"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
